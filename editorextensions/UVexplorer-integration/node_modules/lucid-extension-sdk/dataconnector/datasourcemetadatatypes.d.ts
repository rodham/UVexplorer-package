import { isString, isUnknown } from '../core/checks';
export interface MetadataPatch {
    key: string;
    oldValue: unknown;
    newValue: unknown;
}
export interface MetadataRecord {
    key: string;
    value: unknown;
}
export declare const isSerializedMetadataRecord: (subject: unknown) => subject is import("..").DestructureGuardedTypeObj<{
    key: typeof isString;
    value: typeof isUnknown;
}>;
interface NoContent {
    status: 'NoContent';
}
interface Conflict {
    status: 'Conflict';
    records: MetadataRecord[];
}
export type MetadataPatchResponse = NoContent | Conflict;
export {};
