// import { expect, it, describe } from '@jest/globals';

describe('UVexplorer client tests', () => {
    beforeEach(() => {});
    it('should test something', () => {
        expect(1 + 1).toBe(2);
    });
});
